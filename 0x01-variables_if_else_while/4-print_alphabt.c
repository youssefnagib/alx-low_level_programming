#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: msh 3arf bzabt;
 *
 * return: 0 success;
 *
 */

int main(void)
{
	for (char ch = 'a'; ch <= 'z'; ch++)
	{
		if (ch != 'e' && ch != 'q')
		{
			putchar(ch);
		}
}
putchar('\n');
return (0);
}
