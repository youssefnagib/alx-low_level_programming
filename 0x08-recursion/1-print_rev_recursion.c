#include "main.h"
/**
 * */
void _print_rev_recursion(char *s);
{
	int i = 0;
	int t;

	i++;
	for (s[0] != '\0'; s++; i--)
	{
		i = t;
	}
	for (s[0] != '\0'; s--; t++)
	{
		s[0] == t;
	}
	return 1;
}
